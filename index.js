var fs = require('fs');
var program = require('commander');

// module.exports.readCheck = fs.readFile(program.args[0], { encoding: 'utf8' }, function(err, contents) {
//   console.log(contents);
// });